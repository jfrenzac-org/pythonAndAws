"""Ejercicio 4: Palabras como lista
Enunciado:
Crea una función que reciba una frase (string) y devuelva una lista donde
cada palabra de la frase sea un elemento, usando espacios como separadores."""

frase = ("Estas son las palabras que forman esta frase.")

def frase_en_lista(frase):

    return list(frase.split())
                
lista_espaciada = frase.split("---")

print(lista_espaciada)

## palabras_frase = list(frase.split(dividir_frase_en_lista))

##print(palabras_frase)

#print(list(frase(-)))

##if palabra in frase:
##lista_palabras.append("-")

##for palabra in (palabras_frase):
    ##list(palabras_frase.append([]))
    
## print(lista_palabras)

##def dividir_cadena_en_lista(cadena):
"""
    ##Convierte una cadena de texto en una lista donde cada palabra es un elemento.

    Args:
        cadena: La cadena de texto a dividir.

    Returns:
        Una lista donde cada palabra de la cadena es un elemento.
    """
   ## return cadena.split()



